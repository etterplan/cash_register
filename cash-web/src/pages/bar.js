import React from 'react';

const Bar = ({ guest, setSelectedNameFromBar }) => {
    return (
        <div>
            <h1>Bar Page</h1>
            <h2>{guest}</h2>
        </div>
    );
};

export default Bar;
